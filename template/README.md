# Project README

This repository uses `harness-kit` as a lightweight agent working contract.

Start with `AGENTS.md` for local agent guidance and `harness-kit.yaml` for the structured workflow contract.
