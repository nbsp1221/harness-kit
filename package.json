{
  "name": "@retn0/harness-kit",
  "version": "0.1.0",
  "description": "retn0's opinionated harness-kit starter for repo-local agent workflows.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nbsp1221/harness-kit.git"
  },
  "bin": {
    "harness-kit": "./bin/harness-kit.js",
    "hks": "./bin/harness-kit.js"
  },
  "files": [
    "bin/",
    "template/",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "python -m unittest discover -s tests",
    "package:dry-run": "npm pack --dry-run --json",
    "package:check": "python -m unittest discover -s tests && npm pack --dry-run --json"
  }
}
